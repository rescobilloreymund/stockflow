import { Button } from "@/components/ui/Button";

export default function UserMenu() {
  return (
    <Button type="button" className="rounded-full bg-primary/80" size="icon">
      R
    </Button>
  );
}
